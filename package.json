{
  "name": "movieboyz-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Static site (Vite) for the MovieBoyz platform. Pure renderer of published artifacts.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vitest": "^4.1.10"
  }
}
